* বিজোড় সংখ্যার যোগফল পেতে হলে  n  টি বিজোড় ই হতে হবে 
* জোড় সংখ্যার যোগফল পেতে হলে    n  টি  জোড় ই হতে হবে 
---- Those are the basic of this code.. i just love this code



#include<bits/stdc++.h>
using namespace std;
int main()
{
    int test;
    cin>>test;
    while(test--)
    {
        long long int  n,k;
        long long int s=0;
        cin>>n>>k;

        s=k*k;

       if(n>=s)
       {
           if(n%2==0 && k%2==0)
           {
             cout<<"YES"<<endl;
           }
           else if(n%2!=0 && k%2!=0)
           {
               cout<<"YES"<<endl;
           }
           else
           {
               cout<<"NO"<<endl;
           }

       }

       else
       {
           cout<<"NO"<<endl;
       }


    }
    return 0;
}
